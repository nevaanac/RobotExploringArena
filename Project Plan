# Project Plan

-Arena-
1. Create the grid
2. Generate a code to draw a square with the top left corner given
3. Random function to generate squares throughout the grid (randomize the top left corner)
4. isSquare() function to understand if there is an obstacle or not

-Robot-
1. Generate Robot (triangle)
2. Locate Robot:
robotun olduğu üçgenin MERKEZİNDEN hangi karede olduğunu bul (top left corner)
    1. bulunduğu kare
    2. gideceği kare (if right/left, then ... şeklinde)
bu iki bilgi gerekiyo. robotun şekli, gideceği yöne göre değişecek

3. Implement functions right, left, and forward. (sol üst köşe değişince üçgenin konumu değişsin)
4. Randomize path
    if left&&isSquare(), then randomize again
    while loop??